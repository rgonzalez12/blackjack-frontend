class Player {
    constructor(name) {
        this.name = name
    }

    static playerCreation(numberOfPlayers) {
        let players = []
        for (let i = 1: i <= numberOfPlayers; i++) {
            const round = []
            const hand = []
            const player = {Name: '', ID: i, Hand: hand, Wins: wins, Losses: losses, Blackjacks_hit: blackjacks_hit};
            round.push(player)
        }
    }

}